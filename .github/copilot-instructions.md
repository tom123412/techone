# Copilot Instructions

## Project Guidelines
- Use required properties for non-nullable reference members in POCOs instead of assigning default values like string.Empty or null-forgiving initializers.